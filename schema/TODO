*overkoepelende tabel maken
*naam bedenken voor tabel: PAPAtabel

sub-tabellen moeten door admin aangemaakt gaan worden
*admin moet aan sub-tabellen een DUIDELIJKE EN UNIEKE naam geven
*sub-tabel kan schilderijen of schilders bevatten, dit is aangegeven in de PAPAtabel
*naam van sub-tabel wordt automatisch in PAPAtabel gezet, NIET ALS FOREIGN KEY
*alle gegevens in 1 keer toevoegen aan de sub-tabel



admin kan in PAPAtabel een naam verwijderen, dan wordt eerst de gehele sub-tabel verwijdert voordat de rij in de PAPAtabel wordt verwijdert
gegevens later toevoegen aan baby